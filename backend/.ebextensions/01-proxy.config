files:
  "/etc/nginx/conf.d/proxy.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      client_max_body_size 25M;
      keepalive_timeout 60;

container_commands:
  01_reload_nginx:
    command: "service nginx reload"
